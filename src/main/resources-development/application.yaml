quarkus:
  http:
    port: 8080
    ssl:
      certificate:
        files: META-INF/resources/cert.pem
        key-files: META-INF/resources/cert-key.pem
    http2: true
    ssl-port: 9443
    read-timeout: 600s
  datasource:
    db-kind: h2
    jdbc:
      url: jdbc:h2:./target/output/myDb;create=true
  hibernate-orm:
    log:
      sql: true
    database:
      generation: update
  shutdown:
    timeout: 1
